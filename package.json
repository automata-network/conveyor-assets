{
  "name": "@xata/tokenlist",
  "version": "5.0.0",
  "description": "XATA default token list",
  "repository": "https://github.com/automata-network/conveyor-assets.git",
  "author": "Arvianto Dwi <arvi@ata.network>",
  "license": "GPL-3.0-or-later",
  "main": "dist/xata.tokenlist.json",
  "files": [
    "dist/xata.tokenlist.json"
  ],
  "scripts": {
    "build": "rimraf dist && mkdir -p dist && node scripts/write.js > dist/xata.tokenlist.json"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
